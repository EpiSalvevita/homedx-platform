# HomeDX App Functionalities

The canonical feature matrix and backlog live in **[docs/APP_FUNCTIONALITIES.md](docs/APP_FUNCTIONALITIES.md)**.

That doc covers:

- What the backend and Flutter app implement today
- Gaps per domain (tests, payments, certificates, etc.)
- A prioritized backlog
- REST API conventions (`/gg-homedx-json/gg-api/v1/*`)

There is no separate long-form list at the repo root anymore — one file avoids stale duplicates (old GraphQL names, React Native assumptions, outdated phase plans).
