<?xml version="1.0" encoding="utf-8"?>
<!-- Dev/local HTTP (LAN + WSL); replace with HTTPS or a stricter config for production. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
