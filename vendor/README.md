# Vendor reference (not product code)

Local Cube SDK reference material. Binary trees are gitignored.

- `cube-android/` — Android sample / AAR reference (build uses `frontend/mobile/hdx_mobile/android/app/libs/cubelib-release.aar`)
- `cube-windows/` — Windows DLL and programmer’s guide (reference only)
